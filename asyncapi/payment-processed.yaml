asyncapi: '2.6.0'
info:
  title: Payment Processed Event
  version: '1.0.0'
  description: Evento publicado quando um pagamento é processado
channels:
  payment.processed:
    publish:
      summary: Pagamento processado
      message:
        name: PaymentProcessed
        payload:
          type: object
          properties:
            paymentId:
              type: string
            studentId:
              type: string
            amount:
              type: number
              format: float
            processedAt:
              type: string
              format: date-time
